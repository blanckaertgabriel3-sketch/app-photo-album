agenda 
--------------------------------------------

me 10 juin : 
- Utilisation de la requête, albums_hashtags. Qui créée un lien entre un album et un hashtag.

- Gestion album ERREUR HTTP : Il y a maintenant une vérification d'existance d'hashtag. Sinon il y avait une ERREUR HTTP.
- L'utilisateur ne peut plus mettre 2 hashtags identique. Ce qui provoquait une erreur http, car c'est un élément unique dans la bdd.
- Gestion album, Modification de route création album et du corp de la requête création d'album : Définition de la restriction ; public, privé, restrinct.
- Création route recherche utilisateur pour la collaboration d'album.
- Modification route, login et register sont maintenant des méthodes post.
- Ajout barre de recherche qui permet de trouver noms d'utilisateurs, en vu de les ajouter comme collaborateurs de l'album. Ajout des noms d'utilisateurs dans une liste dynamique.

je 11 juin : 
- Il est possible d'ajouter un collaborateur et de le supprimer d'un tableau, lors de la créatin de l'album. Quand l'utilisateur clique sur un résultat de la barre de recherche pour collaborateur, l'utilisateur est ajouté dans un tableau. 
Ajout dynamique d'élements sous forme de liste pour les résultats de la barre de recherche et les collaborateurs ajoutés à l'album. 
- Création de route api : get albums retourne toutes les info des albums qui sont public, ordoné de maniére ascendante ; get photos_albums retourne toutes les photos dans l'album ; get_photo retourne la photo correspondant à un photo_id.
- Changement position fonctions concernant les photos : upload.php, create_photo.php et search_photo.php dans un fichier photos.php
- Suppression de la ligne session_start() qui était dupliqué dans tous les fichiers .php, par un seul démarage de la session en tête de fichier.

à faire
--------------------------------------------
connexion 

utilisation de createPostElement dans la page home.




- Gestion album : Imposer une limite charactères pour hashtags.

- Gestion album Bug : Barre de recherche, si on spam espace et supp. il y a plein de requêtes qui ce font en même temps. 
Alors il y a plus de résultat de recherche que le max autorisé, les résultats sont dupliqué.

- Gestion photo : Redimensionner et compresser les fichiers après l'upload.

